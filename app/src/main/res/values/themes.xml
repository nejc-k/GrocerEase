<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base Theme -->
    <style name="Base.Theme.GrocerEase" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/lightGreen</item> <!-- Main button color -->
        <item name="android:windowBackground">@drawable/background</item> <!-- Drawable for background -->
    </style>

    <style name="Theme.GrocerEase" parent="Base.Theme.GrocerEase" />

    <!-- Splash Screen Theme -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/customYellow</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen_logo</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.GrocerEase</item>
        <item name="windowSplashScreenAnimationDuration">700</item>
    </style>
</resources>
